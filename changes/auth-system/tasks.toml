[task.db-models]
title = "Database models and migrations"
description = "Create user, session, and token tables"
status = "complete"
requirements = [
  "The system SHALL persist user credentials securely.",
  "WHEN a session is created the system SHALL store the refresh token."
]

[task.auth-endpoint]
title = "Authentication REST endpoint"
description = "POST /auth/login and /auth/logout endpoints"
status = "complete"
requirements = [
  "WHEN the user submits credentials the system SHALL validate them.",
  "IF validation fails THEN the system SHALL return 401 Unauthorized."
]

[task.refresh-tokens]
title = "Refresh token implementation"
description = "Token refresh flow with rotation"
status = "in_progress"
requirements = [
  "WHEN a refresh token is used the system SHALL issue a new access token.",
  "The system SHALL rotate refresh tokens on each use."
]

[task.session-management]
title = "Session management"
description = "Session timeout and invalidation"
status = "pending"
requirements = [
  "WHILE a session is active the system SHALL track last activity.",
  "WHEN session expires the system SHALL require re-authentication."
]

[task.integration-tests]
title = "Integration test coverage"
description = "E2E tests for auth flows"
status = "pending"
requirements = [
  "The system SHALL validate all authentication flows via integration tests."
]
